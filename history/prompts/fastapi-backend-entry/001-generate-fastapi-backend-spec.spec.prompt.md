---
id: 001
title: Generate FastAPI Backend Spec
stage: spec
date: 2026-05-13
surface: agent
model: Gemini 2.0 Flash
feature: fastapi-backend-entry
branch: 007-fastapi-backend-entry
user: Mutahir Bin Athar
command: /sp.specify
labels: ["fastapi", "backend", "specification"]
links:
  spec: specs/007-fastapi-backend-entry/spec.md
  ticket: null
  adr: null
  pr: null
files:
  - specs/007-fastapi-backend-entry/spec.md
  - specs/007-fastapi-backend-entry/checklists/requirements.md
tests:
  - N/A
---

## Prompt

Project: Mutahir Bin Athar — Personal Portfolio Spec: S-7 / FastAPI Backend Entry Point Phase: 1 — Foundation (FINAL SPEC) Depends on: S-1 complete (backend folder scaffold exists) S-6 complete (config.py + settings singleton) Stack: Python FastAPI, Pydantic v2, slowapi, OpenAI Agents SDK, Uvicorn Convention: snake_case filenames, PascalCase classes, snake_case functions — per Constitution Pillar I --- ## OBJECTIVE Implement the FastAPI application entry point and all backend infrastructure shells that the AI agents and routers will plug into in Phase 3 and Phase 4. This spec delivers: 1. main.py — FastAPI app, CORS, routers, health 2. rate_limiter.py — slowapi config (10 req/min/IP) 3. Router shells — chat, contact, projects (stubs only) 4. Model shells — Pydantic request/response models 5. Agent shells — stub files ready for Phase 3/4 6. README.md — backend setup and run instructions The backend must be fully startable after this spec: uvicorn main:app --reload → server starts on port 8000 → GET /api/health returns { "status": "ok" } → all router prefixes registered and responding → no import errors on startup --- ## FILES TO IMPLEMENT backend/ ├── main.py ← FastAPI app entry point ├── routers/ │ ├── __init__.py ← already exists (S-1) │ ├── chat_router.py ← stub with prefix only │ ├── contact_router.py ← stub with prefix only │ └── projects_router.py ← stub with prefix only ├── agents/ │ ├── __init__.py ← already exists (S-1) │ ├── chat_agent.py ← stub only (Phase 4) │ ├── summary_agent.py ← stub only (Phase 3) │ └── contact_agent.py ← stub only (Phase 4) ├── models/ │ ├── __init__.py ← already exists (S-1) │ ├── chat_models.py ← Pydantic models │ └── contact_models.py ← Pydantic models └── lib/ ├── __init__.py ← already exists (S-1) ├── config.py ← already done (S-6) ├── gemini_client.py ← stub only (Phase 3) ├── prompts.py ← stub only (Phase 4) └── rate_limiter.py ← implement fully here --- ## FUNCTIONAL REQUIREMENTS ### FR-001: FastAPI Application Instance In main.py, create the FastAPI app instance: app = FastAPI( title='Mutahir Portfolio API', description='Backend API for mutahir.dev portfolio', version='1.0.0', docs_url='/api/docs', redoc_url='/api/redoc', openapi_url='/api/openapi.json', ) docs_url and redoc_url must be under /api/ prefix to match the Vercel rewrite proxy pattern from S-6. ### FR-002: CORS Middleware Add CORSMiddleware using origins from settings: app.add_middleware( CORSMiddleware, allow_origins=settings.allowed_origins, allow_credentials=True, allow_methods=['GET', 'POST', 'OPTIONS'], allow_headers=['Content-Type', 'Authorization'], ) allow_methods restricted to GET, POST, OPTIONS only — portfolio API never needs PUT, PATCH, or DELETE. Origins come from settings.allowed_origins (S-6) — never hardcoded. ### FR-003: Health Check Endpoint GET /api/health Response model: class HealthResponse(BaseModel): status: str version: str environment: str Implementation: @app.get('/api/health', response_model=HealthResponse) async def health_check() -> HealthResponse: return HealthResponse( status='ok', version='1.0.0', environment=settings.environment, ) This endpoint: - Requires NO authentication - Is exempt from rate limiting - Used by Vercel/Railway health checks in production - Returns environment name for deployment debugging ### FR-004: Router Registration Register all three routers with /api prefix: app.include_router(chat_router, prefix='/api') app.include_router(contact_router, prefix='/api') app.include_router(projects_router, prefix='/api') Final route map: GET /api/health → health_check (main.py) POST /api/chat → chat_router POST /api/contact → contact_router GET /api/projects → projects_router GET /api/projects/summaries → projects_router GET /api/docs → Swagger UI GET /api/redoc → ReDoc UI ### FR-005: Global Exception Handler Register a global handler for unhandled exceptions: @app.exception_handler(Exception) async def global_exception_handler( request: Request, exc: Exception, ) -> JSONResponse: return JSONResponse( status_code=500, content={ 'error': True, 'code': 'INTERNAL_SERVER_ERROR', 'message': 'An unexpected error occurred.', 'detail': str(exc) if settings.debug else None, }, ) detail field: only populated when settings.debug=True Never expose raw exception details in production. ### FR-006: Request ID Middleware Add a middleware that injects a unique request ID into every request for tracing: @app.middleware('http') async def add_request_id( request: Request, call_next: Callable, ) -> Response: request_id = str(uuid.uuid4()) request.state.request_id = request_id response = await call_next(request) response.headers['X-Request-ID'] = request_id return response Import: import uuid Import: from collections.abc import Callable Every response includes X-Request-ID header — useful for debugging production issues. ### FR-007: Startup and Shutdown Events Register lifespan events using asynccontextmanager: from contextlib import asynccontextmanager @asynccontextmanager async def lifespan(app: FastAPI): # Startup print(f'[STARTUP] Mutahir Portfolio API starting...') print(f'[STARTUP] Environment: {settings.environment}') print(f'[STARTUP] Allowed origins: {settings.allowed_origins}') print(f'[STARTUP] Debug mode: {settings.debug}') yield # Shutdown print('[SHUTDOWN] Mutahir Portfolio API stopping...') app = FastAPI(..., lifespan=lifespan) Startup logs must NEVER print sensitive values (gemini_api_key, resend_api_key) — per SR-003 (S-6). ### FR-008: Rate Limiter Setup File: backend/lib/rate_limiter.py Implement using slowapi: from slowapi import Limiter from slowapi.util import get_remote_address from slowapi.errors import RateLimitExceeded from slowapi.middleware import SlowAPIMiddleware from fastapi import Request from fastapi.responses import JSONResponse limiter = Limiter( key_func=get_remote_address, default_limits=['10/minute'], ) async def rate_limit_exceeded_handler( request: Request, exc: RateLimitExceeded, ) -> JSONResponse: return JSONResponse( status_code=429, content={ 'error': True, 'code': 'RATE_LIMIT_EXCEEDED', 'message': f'Rate limit exceeded. ' f'Try again in 60 seconds.', 'retry_after': 60, }, ) Export: limiter (imported by routers and main.py) Export: rate_limit_exceeded_handler In main.py, register rate limiter: from backend.lib.rate_limiter import ( limiter, rate_limit_exceeded_handler, ) app.state.limiter = limiter app.add_middleware(SlowAPIMiddleware) app.add_exception_handler( RateLimitExceeded, rate_limit_exceeded_handler, ) ### FR-009: Router Stubs Each router file must have: - Module docstring - APIRouter instance with correct prefix and tags - At least one stub endpoint returning placeholder - TODO comment for Phase implementation chat_router.py: """Chat router — POST /api/chat (Phase 4).""" from fastapi import APIRouter router = APIRouter(prefix='/chat', tags=['chat']) @router.post('') async def chat() -> dict: # TODO: Implement in Phase 4 using chat_agent.py return {'message': 'Chat endpoint coming soon'} contact_router.py: """Contact router — POST /api/contact (Phase 4).""" from fastapi import APIRouter router = APIRouter(prefix='/contact', tags=['contact']) @router.post('') async def contact() -> dict: # TODO: Implement in Phase 4 using contact_agent.py return {'message': 'Contact endpoint coming soon'} projects_router.py: """Projects router — GET /api/projects (Phase 3).""" from fastapi import APIRouter router = APIRouter(prefix='/projects', tags=['projects']) @router.get('') async def get_projects() -> dict: # TODO: Implement in Phase 3 return {'projects': []} @router.get('/summaries') async def get_summaries() -> dict: # TODO: Implement in Phase 3 using summary_agent.py return {'summaries': []} ### FR-010: Pydantic Request/Response Models chat_models.py: """Pydantic models for chat endpoints.""" from pydantic import BaseModel, Field class ChatMessage(BaseModel): role: str = Field(..., pattern='^(user|assistant)$') content: str = Field(..., min_length=1, max_length=2000) class ChatRequest(BaseModel): message: str = Field( ..., min_length=1, max_length=500, description='User message to the portfolio chatbot', ) history: list[ChatMessage] = Field( default_factory=list, max_length=20, description='Conversation history (max 20 messages)', ) class ChatResponse(BaseModel): reply: str request_id: str contact_models.py: """Pydantic models for contact endpoints.""" from pydantic import BaseModel, Field, EmailStr class ContactRequest(BaseModel): name: str = Field( ..., min_length=2, max_length=100, description='Sender full name', ) email: EmailStr = Field( ..., description='Sender email address', ) message: str = Field( ..., min_length=10, max_length=2000, description='Contact message', ) class ContactResponse(BaseModel): success: bool message: str request_id: str Note: EmailStr requires pydantic[email] extra. Add to requirements.txt: pydantic[email]>=2.7.0 ### FR-011: Agent Stubs Each agent file — stub only, Phase 3/4 implements: chat_agent.py: """ Chat agent — portfolio chatbot. Powered by OpenAI Agents SDK + Gemini 2.5 Flash. Implemented in Phase 4. """ # TODO: Phase 4 — implement ChatAgent # Uses: openai-agents SDK, gemini_client.py, prompts.py summary_agent.py: """ Summary agent — GitHub project summarizer. Powered by OpenAI Agents SDK + Gemini 2.5 Flash. Implemented in Phase 3. """ # TODO: Phase 3 — implement SummaryAgent # Uses: openai-agents SDK, gemini_client.py contact_agent.py: """ Contact agent — smart reply drafter. Powered by OpenAI Agents SDK + Gemini 2.5 Flash. Implemented in Phase 4. """ # TODO: Phase 4 — implement ContactAgent # Uses: openai-agents SDK, gemini_client.py, prompts.py ### FR-012: Library Stubs gemini_client.py: """ Gemini client — OpenAI Agents SDK provider config. Configures Gemini 2.5 Flash as the LLM backend. Implemented in Phase 3. """ # TODO: Phase 3 — configure OpenAI Agents SDK # with Gemini 2.5 Flash as custom model provider prompts.py: """ Agent system prompts — typed constants. Owner: Mutahir Bin Athar Implemented in Phase 4. """ # TODO: Phase 4 — define CHAT_SYSTEM_PROMPT, # CONTACT_SYSTEM_PROMPT as typed string constants ### FR-013: Backend README File: backend/README.md Must contain: # Mutahir Portfolio — Backend API ## Setup 1. cd backend 2. python -m venv venv 3. source venv/bin/activate (Windows: venv\Scripts\activate) 4. pip install -r requirements.txt ## Environment Variables 1. cp .env.example .env 2. Fill in GEMINI_API_KEY, RESEND_API_KEY, ALLOWED_ORIGINS 3. See .env.example for all variables and sources ## Running Development: uvicorn main:app --reload --port 8000 Production: uvicorn main:app --host 0.0.0.0 --port $PORT ## API Reference Swagger UI: http://localhost:8000/api/docs ReDoc: http://localhost:8000/api/redoc ## Route Map GET /api/health → Health check POST /api/chat → AI chatbot (Phase 4) POST /api/contact → Contact form (Phase 4) GET /api/projects → Project list (Phase 3) GET /api/projects/summaries → AI summaries (Phase 3) ## Architecture main.py → FastAPI app, CORS, middleware routers/ → Route handlers (one per domain) agents/ → OpenAI Agents SDK agents (Phase 3/4) models/ → Pydantic request/response models lib/ → Shared: config, rate limiter, prompts --- ## PERFORMANCE REQUIREMENTS PR-001: Server startup time < 2 seconds Measured from uvicorn start to first request ready Verified: uvicorn startup log shows ready message PR-002: Health check response time < 50ms No database or external calls in health check Verified: curl timing on GET /api/health PR-003: Rate limiter overhead < 5ms per request slowapi uses in-memory storage by default — no Redis required for Phase 1 Verified: compare response times with/without SlowAPIMiddleware in place --- ## EDGE CASES EC-001: Port already in use If port 8000 is occupied: uvicorn main:app --port 8001 Document in README.md troubleshooting section EC-002: CORS preflight (OPTIONS) requests allow_methods includes 'OPTIONS' (FR-002) Browser preflight requests handled automatically by CORSMiddleware — no manual OPTIONS handlers needed EC-003: Rate limit on health check Health check endpoint must be EXEMPT from rate limiting — it is called by infrastructure, not users. Apply @limiter.exempt decorator to health_check: @limiter.exempt @app.get('/api/health') async def health_check() -> HealthResponse: ... EC-004: Missing pydantic[email] for EmailStr If pydantic[email] is not installed, importing EmailStr raises ImportError at startup. Resolution: ensure requirements.txt has pydantic[email]>=2.7.0 and pip install is re-run. EC-005: Router import order Python circular import risk if routers import from agents which import from lib which imports from config which imports from routers. Prevention: routers import agents, agents import lib, lib imports nothing internal. Dependency direction: routers → agents → lib Never reverse this direction. EC-006: Debug mode in production settings.debug must be False in production. The global exception handler (FR-005) only exposes raw exception details when debug=True. Verify: ENVIRONMENT=production → debug=False. --- ## SUCCESS CRITERIA SC-001: uvicorn main:app --reload starts without errors SC-002: GET /api/health returns: { "status": "ok", "version": "1.0.0", "environment": "development" } SC-003: X-Request-ID header present on all responses SC-004: CORS headers present on OPTIONS preflight SC-005: POST /api/chat returns 200 with stub response SC-006: POST /api/contact returns 200 with stub response SC-007: GET /api/projects returns 200 with stub response SC-008: GET /api/projects/summaries returns 200 SC-009: GET /api/docs returns Swagger UI (200) SC-010: Rate limiter active — 11th request in 1 minute returns 429 with RATE_LIMIT_EXCEEDED code SC-011: Health check exempt from rate limiting SC-012: Startup logs show environment, origins, debug but NO sensitive keys (gemini, resend) SC-013: Global exception handler returns structured JSON with detail: null when debug=False SC-014: python -m ruff check backend/ returns 0 errors SC-015: All agent and lib stub files have docstrings SC-016: backend/README.md exists with setup instructions --- ## STOP AFTER COMPLETION After printing the S-7 summary block, STOP COMPLETELY. Do NOT generate Phase 2 specs automatically. Do NOT run /sp.plan, /sp.specify, or any other command. Await explicit user instruction before any further action.

## Response snapshot

SUCCESS (spec ready for planning)

## Outcome

- ✅ Impact: High
- 🧪 Tests: N/A
- 📁 Files: spec.md, requirements.md
- 🔁 Next prompts: /sp.plan
- 🧠 Reflection: The input was exceptionally detailed, which allowed for a seamless transition into a structured specification. I focused on translating the technical implementation requirements into user stories and testable functional requirements.

## Evaluation notes (flywheel)

- Failure modes observed: None
- Graders run and results (PASS/FAIL): PASS
- Prompt variant (if applicable): N/A
- Next experiment (smallest change to try): N/A
