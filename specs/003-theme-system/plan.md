# Implementation Plan: S-3 / Theme System

**Branch**: `003-theme-system` | **Date**: 2026-05-05 | **Spec**: [specs/003-theme-system/spec.md](specs/003-theme-system/spec.md)
**Input**: Feature specification for a complete dark/light theme infrastructure.

## Summary

Implement a robust theme system for the portfolio using Tailwind CSS 4 and Next.js 15. The system features persistence via `localStorage`, zero-flash loading (FAWT prevention) via a blocking inline script, and real-time synchronization with OS preferences. Interactivity is provided by a custom `useTheme` hook and an icon-only `ThemeToggle` component.

## Technical Context

**Language/Version**: TypeScript (Strict), Next.js 15 (App Router)
**Primary Dependencies**: Tailwind CSS 4, Framer Motion
**Storage**: `localStorage` (key: `mba-portfolio-theme`)
**Testing**: `tsc --noEmit`, Manual verification per `quickstart.md`
**Target Platform**: Web (Modern Browsers)
**Project Type**: Web application (Frontend + Backend detected, this feature is Frontend-only)
**Performance Goals**: FAWT script < 200 bytes, 60fps animations
**Constraints**: Zero hydration mismatches, No layout-triggering animations
**Scale/Scope**: Application-wide theme infrastructure

## Constitution Check

*GATE: Must pass before Phase 0 research. Re-check after Phase 1 design.*

- [x] **Pillar I (Code Quality)**: Using strict TypeScript, kebab-case filenames, and PascalCase components.
- [x] **Pillar III (Tailwind Consistency)**: Implementing `.dark` class toggle on `<html>` using Tailwind 4.
- [x] **Pillar IV (Performance)**: Using Framer Motion for opacity-only animations; FAWT script size target met.
- [x] **Accessibility**: Implementing keyboard navigation and ARIA labels for the toggle.

## Project Structure

### Documentation (this feature)

```text
specs/003-theme-system/
├── plan.md              # This file
├── research.md          # FAWT script optimization and SSR safety
├── data-model.md        # ThemeState and Persistence Store
├── quickstart.md        # Manual verification guide
└── tasks.md             # To be generated by /sp.tasks
```

### Source Code (repository root)

```text
frontend/
├── app/
│   └── layout.tsx       # RootLayout with FAWT Script
├── components/
│   └── layout/
│       ├── theme-provider.tsx # Theme state provider
│       └── theme-toggle.tsx   # UI component
├── hooks/
│   └── use-theme.ts     # Custom hook with centralized sync
├── types/
│   └── index.ts         # Type definitions
```

**Structure Decision**: Standard Next.js 15 App Router structure with atomic layout components.

## Sequenced Implementation Plan

### Phase A — Update types/index.ts
- **Exact change**: Add `Theme` type and `ThemeHookReturn` interface.
- **Key detail**: Ensure types are exported for consumption by hooks and components.
- **Gotcha**: Avoid circular dependencies by keeping types separate from logic.

### Phase B — Implement hooks/use-theme.ts
- **Exact change**: Create a 'use client' hook that manages theme state.
- **Key detail**: Listen for `prefers-color-scheme` changes. **S1: Centralize `document.documentElement.classList` synchronization in a dedicated `useEffect` to ensure robust state-to-DOM mapping.**
- **Gotcha**: **SSR Trap**: Initialize state to `undefined` and only read `localStorage` inside `useEffect` to prevent hydration mismatches.

### Phase C — Implement components/layout/theme-toggle.tsx
- **Exact change**: Create a 'use client' button component using Framer Motion.
- **Key detail**: Render icons (Moon/Sun) with tooltips.
- **Gotcha**: Ensure the focus ring uses the terminal-style 1px solid, 2px offset per Constitution Pillar III.

### Phase D — Implement ThemeProvider and app/layout.tsx
- **Exact change**: Create `components/layout/theme-provider.tsx` to wrap the app. Inject the FAWT script into `<head>` of `app/layout.tsx`.
- **Key detail**: `ThemeProvider` handles the context; FAWT script must be inlined as a string via `dangerouslySetInnerHTML`.
- **Gotcha**: The script must be blocking to execute before the body is rendered.

### Phase E — Full integration verification
- **Exact change**: Run `tsc --noEmit`, perform manual verification, and check accessibility.
- **Key detail**: **C1: Verify 4.5:1 contrast ratio for all text elements in both modes.**
- **Gotcha**: Check for hydration warnings in the console specifically.
